# 03

数组  

规模n <= 10^5  

找重复  

## 解法

暴力排序后顺序找  
O(n log n) 时间，O(1) 空间  
不要忘了C++ sort的用法  

哈希  
O(n) 时间，O(n) 空间  
使用set实现  
注意set.find()返回的是迭代器  
查找失败返回的是set.end()  

原地交换：利用题目隐藏信息  
数组元素索引与值关系：一对多  
通过将 documents[i] 放到索引 documents[documents[i]] 处  
第二次遇到时即为重复  
